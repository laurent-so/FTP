apt-get update
su
filezilla
apt update
sudo apt update
sudo apt upgrade
ipconfig
ifconfig
ipconfig -a
ifconfig -a
ifconfig.me
curl ifconfig.me
ip a
ip a
sudo nano /etc/proftpd/proftpd.conf
ifconfig a
ip addr
cd /etc/proftpd/
sudo mkdir ssl
cd ssl/
sudo openssl req -x509 -nodes -days 365 -newkey rsa:4096 -out proftpd-rsa.pem -keyout proftpd-key.pem
sudo chmod 440 proftpd-key.pem
cd
sudo chmod 440 proftpd-key.pem
cd ssl/
cd /etc/proftpd/
cd ssl/
sudo nano /etc/proftpd/proftpd.conf
cd
sudo nano /etc/proftpd/proftpd.conf
cd /etc/proftpd/
sudo mkdir ssl
cd ssl/
sudo openssl req -x509 -nodes -days 365 -newkey rsa:4096 -out proftpd-rsa.pem -keyout proftpd-key.pem
sudo chmod 440 proftpd-key.pem
sudo nano /etc/proftpd/proftpd.conf
sudo open ssl req -x509 -nodes -newkey rsa:2048 -keyout /etc/ssl/private/proftpdserver.pem -out /etc/ssl/certs/proftpdcertificate.pem -days 365
sudo open ssl req -x509 -nodes -newkey rsa:2048 -keyout /etc/ssl/private/proftpdserver.pem -out /etc/ssl/certs/proftpdcertificate.pem -days 36
sudo openssl req -x509 -nodes -days 365 -newkey rsa:4096 -out proftpd-rsa.pem -keyout proftpd-key.pem
sudo nano /etc/proftpd/tls.conf
mod_tls
/mod_tls
vim /etc/proftpd/tls.conf
sudo nano /etc/proftpd/tls.conf
proftpd
exec proftpd
/mod_tls
cd
su
ip a
sudo nano /etc/proftpd/tls.conf
cd /erc/proftpd/proftpd
cd /etc/proftpd
/ssl
cd ssl
sudo nano
sudo nano /etc/proftpd/proftpd.conf
sudo /etc/proftpd restart
cd
ip a
ip z
ip a
sudo nano /etc/proftpd/proftpd.conf
cat > ftp.sh
./ftp.sh
sudo ./ftp.sh
cat > ftp.sh
vi ftp.sh
ip a 
ip a
cat ftp.sh
sudo apt upgrade
21
ip a
sudo apt update
